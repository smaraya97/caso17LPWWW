<!-- For preview Ctrl+Shft+V -->
<!DOCTYPE html>
<html lang="en">
<head>
  <title>Inventario</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/inventario.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/inventario.js"></script>
</head>
<body>

<h2>Inventario</h2>
<div class="accordion" id="accordionExample">
  {{#each meds}}
    <div class="accordion-item">
      <h2 class="accordion-header" id="heading{{this.nombre}}">
        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{this.nombre}}" aria-expanded="true" aria-controls="collapse{{this.nombre}}">
          <strong>{{this.nombre}} - {{gramos}}mg</strong>
        </button>
      </h2>
      <div id="collapse{{this.nombre}}" class="accordion-collapse collapse" aria-labelledby="heading{{this.nombre}}" data-bs-parent="#accordionExample">
        <div class="accordion-body">
          <strong>SKU:</strong> {{this.sku}} <strong>Gramos:</strong> {{this.gramos}} <strong>Comprimidos:</strong> {{this.comprimidos}}
        </div>
      </div>
    </div>
  {{/each}}
</div>

</body>
</html>