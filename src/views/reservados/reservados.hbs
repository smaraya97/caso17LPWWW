<!DOCTYPE html>
<html lang="en">

<head>
  <title>Medicamentos Reservados</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>

  <div class="container-fluid p-3 text-black text-left">
    <h1>Medicamentos Reservados</h1>
    <p class="small">Lista de medicamentos reservados</p>
    <div class="accordion" id="accordionExample">
      {{#each reservas}}
      <div class="accordion-item">
        <h2 class="accordion-header" id="heading{{this._id}}">
          <button class="accordion-button" type="button" data-bs-toggle="collapse"
            data-bs-target="#collapse{{this._id}}" aria-expanded="true" aria-controls="collapse{{this._id}}">
            
              <div class="col-10">
                <strong>{{this.rutPaciente}}: {{this.medicamento}}</strong>
              </div>
            
          </button>
        </h2>
        <div id="collapse{{this._id}}" class="accordion-collapse collapse" aria-labelledby="heading{{this._id}}"
          data-bs-parent="#accordionExample">
          <div class="accordion-body">
            <div class="row">
              <div class="col-10">
                <div> <strong>Medicamento:</strong> {{this.medicamento}} - {{this.gramos}}mg </div>
                <div> <strong>Cantidad:</strong> {{this.cantidad}} </div>
                <div> <strong>Fecha Reserva:</strong> {{this.fechaReserva}} </div>
              </div>
              <div class="col-2">
                <form action="/delReserva" method="post">
                  <button name="id" class="btn btn-success" value="{{this._id}}">Confirmar Entrega</button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</body>

</html>